export type Any = any

export type Currencies = {
    rub: number,
    usd: number
}

export type Props = {
    title: string,
    count?: number
}

export type Link = {
    title: string;
    link: string;
    element: React.ReactNode;
}