import React, { useState } from 'react'
import { Header } from '@components/Header'

export const Home: React.FunctionComponent = () => {
    return (
        <div>
            <Header title='Добро пожаловать на сервер' />
        </div>
    );
}